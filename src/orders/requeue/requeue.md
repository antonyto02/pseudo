*Algoritmo:*

En este archivo se define la función requeue().
Por ahora solo se deja creada la función, sin lógica interna.


-------------------------------------------------------------------------------

    function requeue()

        // TODO: implementar lógica de requeue
        return

-------------------------------------------------------------------------------
